# Система управления дискуссионным клубом

## Идея проекта

Это веб-приложение для управления дискуссионным клубом, которое позволяет:
- Управлять участниками турниров
- Управлять членами жюри
- Создавать и управлять темами дебатов
- Организовывать сезоны и турниры
- Проводить раунды с выступлениями участников
- Оценивать выступления по критериям (логика, риторика, эрудиция)
- Выполнять аналитические запросы

## Архитектура

### Технологии
- **Backend**: Go (Golang)
- **База данных**: PostgreSQL
- **Frontend**: HTML шаблоны (Go templates)
- **Стили**: CSS

### Структура проекта
```
├── main.go                 # Точка входа, настройка сервера
├── internal/
│   ├── database/          # Подключение к БД
│   ├── handlers/          # HTTP обработчики
│   ├── models/            # Модели данных
│   └── repository/        # Слой доступа к данным
├── templates/             # HTML шаблоны
├── triggers.sql           # Триггеры базы данных
├── seed.sql              # Тестовые данные
└── README.md             # Документация
```

## Зачем нужны триггеры в базе данных?

Триггеры - это автоматические процедуры, которые выполняются при определенных событиях (INSERT, UPDATE, DELETE). В этом проекте они решают важные задачи:

### 1. **Автоматическое обновление статуса турнира** (`update_tournament_status`)
- **Проблема**: Статус турнира (upcoming/active/completed) должен обновляться автоматически на основе дат
- **Решение**: Триггер проверяет даты при создании/изменении турнира и автоматически устанавливает правильный статус
- **Польза**: Не нужно вручную обновлять статусы, система всегда показывает актуальное состояние

### 2. **Валидация данных** (`validate_season_dates`, `validate_tournament_dates`)
- **Проблема**: Пользователь может случайно указать неправильные даты (например, дата окончания раньше даты начала)
- **Решение**: Триггеры проверяют корректность данных ДО их сохранения
- **Польза**: Невозможно сохранить некорректные данные, система защищена от ошибок

### 3. **Логирование изменений** (`log_participant_changes`)
- **Проблема**: Нужно отслеживать, кто и когда изменял данные участников
- **Решение**: Триггер автоматически записывает все изменения в таблицу аудита
- **Польза**: Полная история изменений для аудита и восстановления данных

### 4. **Валидация оценок** (`calculate_total_score`)
- **Проблема**: Оценки должны быть в диапазоне 0-10
- **Решение**: Триггер проверяет диапазон перед сохранением
- **Польза**: Гарантия корректности данных, невозможность ввести неверные оценки

### 5. **Уникальность номеров раундов** (`validate_round_number`)
- **Проблема**: В одном турнире не может быть двух раундов с одинаковым номером
- **Решение**: Триггер проверяет уникальность номера в рамках турнира
- **Польза**: Целостность данных, невозможность создать дубликаты

### 6. **Автоматическая установка дат** (`set_created_at`)
- **Проблема**: Нужно автоматически записывать дату создания записи
- **Решение**: Триггер устанавливает `created_at` при создании
- **Польза**: Не нужно вручную указывать даты, они всегда корректны

## Преимущества использования триггеров

1. **Целостность данных**: Невозможно сохранить некорректные данные
2. **Автоматизация**: Многие операции выполняются автоматически
3. **Безопасность**: Валидация на уровне БД, а не только в приложении
4. **Аудит**: Полная история изменений
5. **Производительность**: Валидация происходит в БД, быстрее чем в приложении

## Основные функции системы

### CRUD операции
- ✅ **Create** - создание записей
- ✅ **Read** - просмотр списков и деталей
- ✅ **Update** - редактирование записей
- ✅ **Delete** - удаление записей

### Поиск и фильтрация
- 🔍 Поиск по имени, фамилии, email
- 🔍 Фильтрация по различным критериям
- 🔍 Сортировка данных

### Аналитика
- 📊 Итоговые результаты турниров
- 📊 Рейтинг участников
- 📊 Статистика по темам
- 📊 Средние оценки по критериям

## Как запустить

1. Установите PostgreSQL и создайте базу данных `debate_club`
2. Выполните `triggers.sql` для создания триггеров
3. Выполните `seed.sql` для заполнения тестовыми данными
4. Запустите приложение: `go run main.go`
5. Откройте браузер: `http://localhost:8080`

## Структура базы данных

- **participants** - участники турниров
- **judges** - члены жюри
- **topics** - темы дебатов
- **seasons** - сезоны
- **tournaments** - турниры
- **rounds** - раунды турниров
- **performances** - выступления участников
- **participants_audit** - журнал изменений участников (создается триггером)


